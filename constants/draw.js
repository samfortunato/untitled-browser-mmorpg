/** The actual size of tiles from the asset image itself. */
export const TILE_SIZE_RAW = 16;

/** The tile size when rendered onto the screen. */
export const TILE_SIZE_RENDERED = 32;

/** Fixed game viewport dimensions. */
export const VIEWPORT_W = 960;
export const VIEWPORT_H = 540;
