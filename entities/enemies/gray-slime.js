import { Entity } from '../entity.js';

/**
 * Extremely rare spawn. Drops a Reflect Putty when killed.
 *
 * Maybe make it spawn in random places. Change the spawn place randomly.
 *
 * Should be dark gray. Maybe make it slightly different from other slimes.
 * Maybe put a smile on it, haha.
 * */
export class GraySlime extends Entity { }
